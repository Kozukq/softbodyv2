# Build and run in VSCode with CMakeTools
- Select target as simulation/CMakeLists.txt
- Select a kit for compilation
- Configure the project
- Build the project
- Run at ${workspaceFolder}/simulation via "cd .." through CLI